# Review Decision Log — agent-harness

### plan r1 (codex)

사람 결정: `as proposed` — 여덟 행 모두 accept. 균일한 accept는 보통 도장 찍기의 신호라
각 행을 따로 따졌고, F5에서는 근거 한 절이 사실과 다른 것을 표면에 올렸다(아래).
나머지 일곱은 스펙이 실제로 비워 둔 자리였다.

F1 accept — Open question: What makes a package script a gate?
F2 accept — Open question: How are build-artifact gates ordered?
F3 accept — Browser-backed gate tests contradict browserless CI
F4 accept — D13 promises floating-promise detection without the required type information
F5 accept — The accessibility scope permits known and replacement regressions
F6 accept — The manifest gate permits unsupported Chrome versions
F7 accept — The overflow gate can pass before the seeded UI renders
F8 accept — The format-only proof does not inspect the change

#### F5의 근거 한 절은 사실과 다르다 — 그래도 결론은 유효하다

F5는 "No accessibility rule set … closes this gap"이라고 적었으나 스펙은 jsx-a11y 36규칙을
명시하고 있었다. 규칙 세트는 있었다. 그러나 finding의 핵심은 규칙 세트의 부재가 아니라
**베이스라인의 모양**이었고 그것은 맞다 — 총 개수를 재면 하나를 고치고 하나를 새로 만드는
변경이 총계를 그대로 둔 채 통과한다. 그래서 accept하되 지문(규칙+파일+위치) 단위 베이스라인을
채택하고, 개수가 같은 교체가 FAIL임을 증명하는 케이스를 픽스처 원장에 넣었다.

> **이 지문 형태는 r2에서 뒤집혔다.** 위치를 넣으면 전면 포맷 커밋이 모든 지문을 무효화한다 —
> R2-F3 참조. 최종 형태는 `규칙 + 파일 + 정규화된 대상 식별자`이고 위치를 담지 않는다.
> 이 문단은 r1 시점의 결정 기록으로 남긴다.

#### 스펙에 반영된 것

- **F1 → D4.** `scripts/gates.txt`를 필드 있는 레지스트리로 정의했다(`id`/`script`/`kind`/
  `ci`/`needs`/`na`). 네 자리 일치는 **등록된 게이트에 대해서만** 성립하고, `dev`·`zip`·
  `storybook`·`postinstall` 같은 평범한 스크립트는 검사에 참여하지 않는다 — "모든 스크립트가
  표에 있어야 한다"는 영구 빨강이라 검사가 아니라 소음이었다. 고아 게이트 검출이 선언된 이름
  규약(`*-gate.mjs`) 안에서만 성립한다는 **경계도 함께 적었다**. 이름 휴리스틱을 넓히면
  평범한 스크립트를 오탐하므로 닫는 대신 밝힌다.
- **F2 → D4a(신설).** 산출물을 읽는 게이트 다섯의 실행 순서를 DAG로 고정했다. 러너가 산출물
  디렉터리를 지우고 빌드를 한 번 돌린 뒤 그 산출물에 대해서만 소비자를 돌린다. 빌드 실패 시
  소비자는 `BLOCKED`이며 `FAIL`도 `N/A`도 아니다. 낡은 산출물이 통과를 만들지 못함을 재현하는
  픽스처를 원장에 넣었다.
- **F3 → D12.** 테스트 명령을 `test`(브라우저 없음, CI가 돈다)와 `test:browser`(로컬)로 갈랐다.
  seam은 여전히 하나이고 갈라진 것은 명령이다. 분류가 맞는지 검사하는 parity 테스트를 요구한다.
- **F4 → D13.** "타입 없이 잡히는 것"의 경계를 정확히 적었다. 떠도는 프로미스는 타입 인식
  린팅을 요구하므로 `.mjs`에서 잡히지 않는다 — 스펙 초안이 반대로 적었다. 검출은 `src/`의
  이야기로 옮기고, 그 규칙이 정말 도는지 증명하는 실패 픽스처를 요구한다.
- **F5 → D13a(신설).** 지문 베이스라인. 그리고 이 게이트가 **주지 않는 것**(관측된 키보드
  조작·접근명 보장)을 범위 밖에 명시했다. 스토리 13·14를 게이트가 실제로 주는 것에 맞춰 다시 썼다.
- **F6 → D10.** 최소 크롬 버전을 존재 검사에서 **선언된 하한과의 정확한 일치**로 바꿨다.
  하한 미만·경계·비숫자·부재 네 픽스처를 요구한다.
- **F7 → D9.** 오버플로 훑기 전에 관측 가능한 준비 표지를 요구한다 — 심은 프로필 전부가
  렌더됐다는 것, 지연 로드가 들어왔다는 것. 준비 표지가 제한 시간 안에 나타나지 않으면
  그 자체가 `FAIL`이다. 빈 트리가 통과할 수 없음을 증명하는 케이스를 요구한다.
- **F8 → D8.** `git show --stat`을 증거에서 뺐다. 대신 포맷 커밋의 부모에 고정 버전 포매터를
  다시 돌려 결과 트리가 그 커밋의 트리와 정확히 같은지 잰다. 의미 있는 수정을 섞으면 이 증명이
  실패하는 케이스를 요구한다.

#### 관통하는 것

F2와 F7은 같은 부류다 — **검사하지 않으면서 초록을 내는 게이트**. 낡은 산출물을 재는 매니페스트
게이트와 빈 화면을 훑는 오버플로 게이트는 둘 다 "돌았고 통과했다"고 보고한다. 이 저장소는 이미
같은 실패를 겪었고 그 기록이 `audit-smoke-barriers.mjs`의 헤더와 `i18n-coverage.test.ts`의
주석에 남아 있는데, 새 게이트를 설계하면서 그 교훈을 적용하지 않은 것이 스펙의 결함이었다.

### plan r2 (codex)

r1 재검증 결과: **8건 중 5건 resolved**(F1·F4·F5·F6·F7), 3건 still-open(F2·F3·F8),
그리고 F5를 고치면서 만든 새 high 하나. 사람 결정: `as proposed` — 네 행 모두 accept.
넷 중 셋이 내 수정의 결함이라 반박할 자리가 없었다.

R2-F1 accept — Open question: How are concurrent runner outputs isolated?
R2-F2 accept — Open question: What defines the browser-backed test set?
R2-F3 accept — Open question: How do accessibility fingerprints survive formatting?
R2-F4 accept — The format-only proof still accepts evidence that does not inspect the change

#### 무엇이 잘못됐었나

- **R2-F1(= r1 F2 잔여).** D4a는 **한 번의 호출 안에서만** 순서를 세웠다. 산출물 경로가
  하나뿐이라 러너 둘이 겹치면 A의 소비자가 B의 산출물을 잰다 — D4a가 막으러 온 실패가
  다른 문으로 들어온다. r1의 권고가 "concurrently attempted builds" 픽스처를 명시했는데
  내가 원장에 옮기지 않았다. 지우기–빌드–소비 구간 전체를 잠그고, 겹친 실행이 거절되는 것과
  죽은 잠금이 회수되는 것을 픽스처로 둔다.
- **R2-F2(= r1 F3 잔여).** 명령은 갈랐는데 **두 집합의 정의를 아무 데도 두지 않았다.**
  그래서 내가 세운 parity 단언은 자기 자신을 근거로 삼는 순환이었다 — "브라우저 없는 집합"에
  출처가 없었다. `*.browser.test.mjs` 규약을 정하고 두 glob을 거기서 배타적으로 파생시킨다.
- **R2-F3(신규, 내 수정이 만든 것).** F5를 고치며 지문 베이스라인을 도입했는데, 지문에 줄·열이
  들어가면 D8의 전면 포맷 커밋이 **모든 지문을 한꺼번에 무효화**한다. 그러면 게이트가 포맷 전용
  커밋을 거짓으로 막고, 그것을 푸는 손쉬운 길인 베이스라인 재취득이 **진짜 교체 회귀를 함께
  축복한다** — F5가 막으러 온 그것이다. 두 결정이 각각은 맞는데 만나는 자리가 틀렸다.
  지문을 위치에 무관하게(`규칙 + 파일 + 정규화된 대상 식별자`) 잡고, 베이스라인 취득을
  **포맷 적용 뒤**로 못박는다.
- **R2-F4(= r1 F8 잔여).** D8에서 뺀 `git show --stat` 주장이 Testing Decisions에 그대로
  살아 있었다. 문서 두 곳이 다른 말을 하면 느슨한 쪽이 이긴다 — 스펙을 따르는 구현자가
  무효로 판정된 증거를 그대로 만족시킬 수 있었다.

#### 이 라운드가 남긴 교훈

r1의 findings를 고치는 일이 **새 결함 하나를 만들었다**(R2-F3). 그리고 셋 중 둘은 고침이
불완전했던 것이다 — 한 곳을 고치고 그 주장을 인용하던 다른 곳을 놓쳤다. 원본 하네스의 결정
기록에 같은 항목이 있다: "코어 문장을 지우면 인용이 남는다." 지운 문구로 저장소를 grep하는
것이 습관이 되어야 하고, 이번엔 그것을 하지 않아 R2-F4가 나왔다.

### plan r3 (codex)

사용자가 명시적으로 승인한 라운드(상한 2 초과). r2 재검증 결과: **네 건 모두 resolved.**
새로 도입한 두 기계장치에서 각각 high 하나. 사람 결정: `as proposed` — 두 행 모두 accept.

R3-F1 accept — Open question: When is a dead runner's lock actually stale?
R3-F2 accept — Browser parity cannot see browser use through the mandated child-process seam

#### R3-F1 — 잠금을 강화하지 않고 잠글 것을 없앴다

권고는 두 갈래였다: (a) 프로세스 그룹 소유권으로 잠금을 확장해 모든 후손이 멈췄음을 확인하고
회수하거나, (b) 회차별 고유 산출물 디렉터리를 쓰거나. **(b)를 택했다.**

(a)가 틀린 길인 이유는 지적 자체가 말해 준다 — "소유자가 죽었다"는 "쓰는 것이 다 멈췄다"를
뜻하지 않는다. 러너가 죽어도 빌드 자식은 살아남아 계속 쓴다. 그것을 닫으려면 후손 추적과
drain 판정과 PID 재사용까지 들어오는데, 그건 **또 하나의 부정 증명**이고 이 스펙이 이미 세 번
인용한 그 비용 구조다(원본 하네스의 `@import` 증명, 판정 불가 버전 범위, 기억으로 쓴 allowlist).
경로를 나누면 잠글 것도 회수할 것도 없다 — 살아남은 자식이 쓰는 곳은 자기 회차의 디렉터리다.

대가는 소비자 다섯이 경로를 인자로 받아야 한다는 것이고, 그것은 부수적으로 **원장의 산출물
픽스처들이 실제 빌드 없이 성립하게** 만든다. 픽스처는 "잠금이 회수되는가"에서 "죽인 러너의
자식이 계속 쓰는데도 다음 회차가 영향받지 않는가"로 바뀌었다 — 잠금 설계였다면 정확히
뚫렸을 케이스다.

#### R3-F2 — 내 seam 결정이 내 parity 검사를 무력화하고 있었다

스펙은 게이트 테스트가 대상을 **자식 프로세스로** 띄우라고 요구한다. 그러면 그 테스트는
playwright를 import하지 않는다 — `vitest`와 `child_process`만 import하고 브라우저는 띄워진
게이트가 띄운다. import를 세는 분류는 **스펙이 강제하는 바로 그 형태를 못 본다.** 오탐이 아니라
미탐이고, 하필 규정된 경로에서만 난다. 결과는 브라우저 없는 CI가 크롬을 띄우려다 죽는 것.

분류를 레지스트리의 새 `browser` 필드에서 파생시킨다: `browser: yes`인 게이트를 자식
프로세스로 띄우는 테스트 파일은 `*.browser.test.mjs`여야 한다. 레지스트리는 이미 "무엇이
게이트인가"의 SSOT이므로 분류가 두 번째 진실 원천을 만들지 않는다. parity 픽스처의 핵심은
**playwright를 직접 import하지 않으면서 브라우저 게이트를 띄우는, 이름이 잘못된 테스트**가
FAIL을 내는 것이다 — import를 세는 분류였다면 통과했을 자리.

#### 세 라운드의 추이

findings 8 → 4 → 2. r1과 r2의 수정은 각각 다음 라운드의 findings를 만들었고(둘 다 새 기계장치를
**더하는** 수정이었다), r3의 수정 둘은 반대로 기계장치를 **없애거나**(잠금 → 경로 분리) 이미
있는 SSOT를 재사용한다(레지스트리 `browser` 필드). 이 계보에서 수렴했던 쪽은 늘 후자였다.

지운 문구 grep을 이번에는 수정 직후에 돌렸고(`잠`·`playwright`·`.output/chrome-mv3`),
남은 인용이 전부 "버린 대안"의 서술뿐임을 확인했다. R2-F4를 낳은 습관의 부재를 메운 것이다.

#### 게이트 종결

WAIVED by user: r3 이후 재검토를 면제한다. 근거는 추이의 **질**이다 — findings가 8→4→2로
줄었을 뿐 아니라, r1·r2의 수정은 기계장치를 더해 각각 다음 라운드의 결함을 낳은 반면 r3의
수정 둘은 기계장치를 없애거나(잠금 → 회차별 경로) 이미 있는 SSOT를 재사용한다(레지스트리
`browser` 필드). 남은 위험은 r3 수정이 새로 만드는 표면 — 소비자 다섯의 경로 인자화 — 이며,
그것은 스펙 문서가 아니라 **실제 코드에서** 드러나는 종류라 첫 티켓 뒤의 structure 게이트가
보는 것이 더 정확하다. 이 면제는 플랜 게이트에만 적용되며 structure·release 게이트는 그대로
돈다.

## 구현 중 결정

### 티켓 01 — `audit-smoke-barriers`의 '등록하지 않는다'를 개정한다

그 스크립트의 헤더는 자신이 `package.json`에 등록되지 않는 것이 **의도**라고 적고 있었다.
근거는 "인자로 다른 리비전의 사본을 검사할 수 있다"였다. 개정한다.

근거가 등록을 막지 못한다. 등록해도 인자는 그대로 넘어간다 — `bun run smoke-barriers
/tmp/old-smoke.mjs`가 실제로 그 파일을 검사하는 것을 확인했다. 반면 미등록의 비용은 실재했다:
이 감사가 막는 것은 smoke의 단언이 한 테스트씩 밀리는 결함인데, 사람이 기억해야 돌아가면
기억하지 않은 날 무방비해진다. 게이트 표에 소유자가 생기는 이 티켓이 그것을 고칠 자리다.

`docs/agents/domain.md`가 "기존 결정과 어긋나면 조용히 덮지 말고 표면에 올리라"고 하므로
여기 적는다. 헤더의 해당 문단도 함께 고쳐, 등록됐다는 사실과 인자 사용법을 같이 적었다.

### 티켓 01 — 레지스트리에 `deferred:` 레코드를 더한다 (스펙에 없던 것)

D4의 스키마에는 없다. 그런데 없으면 이 티켓이 **자기 게이트를 통과하지 못한다**:
`bundle-gate.mjs`와 `writer-lane-gate.mjs`가 이미 존재하고 `*-gate.mjs` 규약에 걸리는데,
둘은 빌드 산출물을 읽으므로 티켓 02 전에는 등록할 수 없다. 고아 게이트 검사가 즉시 빨강이 된다.

그래서 "아직 등록하지 않는다"를 **선언**하게 한다. 원본 하네스가 같은 문제를
`known-absent-skills.txt`로 푼 것과 같은 모양이고, 같은 이유로 양방향이다: 등록된 뒤에도
선언이 남아 있으면 FAIL, 존재하지 않는 파일을 선언해도 FAIL. 선언 한 줄로 검사를 침묵시킬 수
있다는 것이 이 장치의 위험이고, 그 위험을 줄이는 것은 선언이 **기록으로 남고 지우는 것이
등록하는 변경의 일부**라는 점이다.

### 티켓 01 — 코드 리뷰가 잡은 것 중 원인이 달랐던 하나

Spec 축이 "1MB `maxBuffer` 때문에 출력 많은 게이트가 거짓 FAIL을 낸다"고 보고했다. 재현되지
않았다 — `bun run`을 거치면 ENOBUFS가 나지 않는다. 그러나 **결론은 옳았고 원인이 두 겹이었다**:

1. 파이프로 캡처하면 `bun run`이 게이트 출력을 약 64KB에서 조용히 자른다 (실측: 2MB →
   65,697바이트). 파일로 받으면 2,097,354바이트가 온전히 남는다.
2. 그것을 고친 뒤 테스트가 러너 자신의 결함을 하나 더 잡았다. `fail()`이 큰 출력을 쓴 직후
   `process.exit()`을 부르는데, 파이프로의 `stdout.write`는 비동기라 아직 나가지 않은 데이터가
   잘린다. 이 저장소의 다른 게이트들이 `process.exitCode`를 쓰는 이유와 같다.

둘 다 "출력을 버리지 않고 보고한다"를 어기고 있었고, 둘 다 조용했다. 보고된 원인이 틀렸다고
결론까지 버렸다면 남았을 결함이다.
