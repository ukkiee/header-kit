# 프로필 스키마는 자체 형식만 지원한다

타사 헤더 확장의 export JSON을 받아들이는 import 변환기를 두지 않는다. 기존 사용자 이주 경로를 얻는 대신 타사 스키마의 모호한 의미론(치환 문법, 레벨별 append 이력 등)과 브랜드 결합을 떠안는 트레이드오프였고, 스키마 주권 — DNR 의미론과 1:1로 대응하는 명확한 필드 — 과 저장소의 브랜드 중립성을 우선했다. export/import는 자체 스키마 단일 형식이다.
