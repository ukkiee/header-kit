# 06 — 적용-규칙-수 배지 + 표시 토글

**What to build:** 툴바 배지를 활성 프로필 표시기에서 **적용 규칙 수 카운터**로 바꾼다(디자인 라벨 "적용 중인 규칙 수"). 설정에서 배지 표시를 끄고 켤 수 있다.

**Blocked by:** 01 (설정 화면 셸).

**Status:** done

- [ ] `computeBadge`가 재조정된 컴파일 요약의 **적용 규칙 수**를 낸다: 적용 0→빈 텍스트, 일시정지→'II'(일시정지색), N개→N
- [ ] quota·컴파일로 일부 규칙이 빠진 경우(부분 스킵)에도 배지는 **실제 적용된 수**를 보여 라벨과 값이 어긋나지 않는다
- [ ] 설정에 배지 표시 on/off 토글(영속), 토글은 표시 여부만 제어
- [ ] core 테스트: 0·일시정지·부분 스킵·quota 실패·표시 꺼짐 상태 (prior art: badge.test.ts)
- [ ] 전 게이트 그린
