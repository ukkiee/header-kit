# 10 — README

**What to build:** 처음 이 저장소를 여는 사람이 코드를 열기 전에 방향을 잡는다. 지금 README가 없어 무엇을 보는 중인지 알 방법이 없다.

이 문서의 주된 일은 **초록이 무엇을 뜻하는지 과장하지 않는 것**이다. 게이트가 무엇을 보장하는지만 적으면 읽는 사람은 나머지도 보장된다고 읽는다. 그래서 각 게이트에 대해 **무엇은 보장하지 않는지**를 함께 적는다 — 매니페스트 게이트는 권한 집합이 선언과 같다는 것을 증명하지 현재 권한 집합이 옳다는 것을 증명하지 않고, 접근성 지문은 새 진단이 없다는 것을 증명하지 키보드로 전체 흐름을 조작할 수 있다는 것을 증명하지 않는다.

**Blocked by:** 09

**Status:** ready-for-agent

- [ ] 이 저장소가 무엇인지 — 프로필 기반으로 HTTP 요청/응답을 수정하는 Chromium 확장 — 를 첫 문단이 말한다
- [ ] 어떻게 돌리는지: 설치, `bun run gate`, 개별 게이트, 브라우저가 필요한 것과 아닌 것
- [ ] 게이트 표의 요약과, **각 게이트가 보장하지 않는 것**
- [ ] 자문 행이 무엇이고 왜 완료를 막지 않는지
- [ ] 하네스 코어가 어디 사는지와 왜 인라인인지(ADR 0018을 가리킨다)
- [ ] 알려진 빈틈을 적는다: 기존 게이트 셋(`bundle-gate`·`writer-lane-gate`·`smoke-barriers`)에 테스트가 없다는 것, `scripts/`가 타입 프로그램 밖이라 그곳에서는 떠도는 프로미스가 잡히지 않는다는 것, 접근성 기존 위반이 베이스라인으로 남아 있다는 것
- [ ] 문서가 주장하는 명령과 게이트 이름이 실제 레지스트리와 일치한다
